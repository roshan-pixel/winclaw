# ============================================================
# WinClaw MCP Servers - Environment Variables Template
# ============================================================
# 1. Copy this file: copy .env.example .env
# 2. Fill in your actual values in .env
# 3. NEVER commit your .env file to Git!
# ============================================================

# Anthropic API Key (REQUIRED for LLM features)
# Get yours at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# LLM Model (claude-3-haiku is fastest & cheapest)
MODEL=claude-3-haiku-20240307

# ── Gateway authentication (REQUIRED) ────────────────────────────────────────
# Comma-separated list of strong API keys. The gateway refuses to start
# without this. Generate with: python -c "import secrets; print(secrets.token_hex(32))"
WinClaw_API_KEYS=change-me-generate-a-strong-key

# Gateway bind address (default: 127.0.0.1 – localhost only; change only if
# you understand the exposure risk and have firewall/VPN controls in place)
WinClaw_BIND_HOST=127.0.0.1

# Gateway port
WinClaw_PORT=8000

# CORS allowed origins (comma-separated; default: localhost only)
# WinClaw_CORS_ORIGINS=http://localhost,http://127.0.0.1

# Request size cap in bytes (default: 5 MB)
# WinClaw_MAX_REQUEST_BYTES=5242880

# Rate limiting: max requests per window per client IP
# WinClaw_RATE_LIMIT_REQUESTS=60
# WinClaw_RATE_LIMIT_WINDOW=60

# ── Shell tool destructive-command guard ─────────────────────────────────────
# Set to true to require confirmed=true for destructive commands
# (Remove-Item, del, format, shutdown, etc.)
SHELL_REQUIRE_CONFIRM=false

# ── WhatsApp Bridge ───────────────────────────────────────────────────────────
WHATSAPP_BRIDGE_PORT=5001

# ── Ollama (Local LLM - Optional) ─────────────────────────────────────────────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=deepseek-r1

# ── Admin / God Mode ─────────────────────────────────────────────────────────
# Set to true for elevated Windows automation. Triggers UAC on launch.
GODMODE=false
ADMIN_MODE=false

# ── Logging Level: DEBUG | INFO | WARNING | ERROR ─────────────────────────────
LOG_LEVEL=INFO
