# Core MCP protocol
mcp>=1.0.0

# Web framework & server (gateway)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# HTTP client libraries
requests>=2.25.0
aiohttp>=3.8.0

# LLM clients
anthropic>=0.20.0
litellm>=1.0.0

# Environment & config
python-dotenv>=0.19.0

# Windows automation (requires Windows)
pywin32>=306; sys_platform == "win32"
pyautogui>=0.9.54; sys_platform == "win32"
Pillow>=10.0.0; sys_platform == "win32"

# Flask (legacy bridge server)
flask>=2.0.0
